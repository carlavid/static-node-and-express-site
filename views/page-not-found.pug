extends layout

block content 
    div.error-message
        h1= `${err.status} - Page Not Found`
        h2= err.message

    div    
        nav.nav
            a.back-link(href='/')
                span &larr;
                |  Back to Home
extends layout 

block content 
    div.error-message
        h1= 'Server Error'
        h2= `Error status code: ${err.status} - Server Error`
        p= err.message
        pre= err.stack

    div
        nav.nav
            a.back-link(href='/')
                span &larr;
                |  Back to Home